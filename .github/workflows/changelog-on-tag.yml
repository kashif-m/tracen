name: Changelog on tag

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to generate changelog for, for example v0.1.7
        required: true
  repository_dispatch:
    types:
      - release-tagged
  push:
    tags:
      - 'v[0-9]*'

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: changelog-${{ github.event.client_payload.tag || inputs.tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  changelog:
    name: changelog
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
          token: ${{ github.token }}
          persist-credentials: false
      - uses: taiki-e/install-action@just

      - name: Append changelog entry if needed
        env:
          GITHUB_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.client_payload.tag || inputs.tag || github.ref_name }}
        run: just workflow-changelog-on-tag
